[project]
name = "pkm-cli-app"
version = "0.4.32"
description = "Command line interface for PKM"
readme = "README.md"
requires-python = ">=3.8, <4.0"
license = {text = "MIT License"}
scripts = {pkm = "pkm_cli.main:main"}

[[project.authors]]
name = "bennyl"

[tool.pkm.application-installer]
app = "pkm-cli ==0.4.32"

[build-system]
requires = [ "pkm >=0.4.23"]
build-backend = "pkm.api.buildsys"