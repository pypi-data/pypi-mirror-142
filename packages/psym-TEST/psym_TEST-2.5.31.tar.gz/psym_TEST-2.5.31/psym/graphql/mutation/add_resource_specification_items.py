#!/usr/bin/env python3
# @generated AUTOGENERATED file. Do not Change!

from dataclasses import dataclass, field as _field
from ...config import custom_scalars, datetime
from gql_client.runtime.variables import encode_variables
from gql import gql, Client
from gql.transport.exceptions import TransportQueryError
from functools import partial
from numbers import Number
from typing import Any, AsyncGenerator, Dict, List, Generator, Optional
from time import perf_counter
from dataclasses_json import DataClassJsonMixin, config

from ..input.add_resource_specification_items_input import AddResourceSpecificationItemsInput


# fmt: off
QUERY: List[str] = ["""
mutation addResourceSpecificationItems (
  $input: AddResourceSpecificationItemsInput!
) {
	addResourceSpecificationItems(input: $input) {
		id
    resourceSpecificationRelationship{
      id
      name
    }
    resourceSpecification{
      id
      name
    }
	} 
}

"""
]


class addResourceSpecificationItems:
    @dataclass(frozen=True)
    class addResourceSpecificationItemsData(DataClassJsonMixin):
        @dataclass(frozen=True)
        class ResourceSpecificationItems(DataClassJsonMixin):
            @dataclass(frozen=True)
            class ResourceSpecificationRelationship(DataClassJsonMixin):
                id: str
                name: str

            @dataclass(frozen=True)
            class ResourceSpecification(DataClassJsonMixin):
                id: str
                name: str

            id: str
            resourceSpecificationRelationship: ResourceSpecificationRelationship
            resourceSpecification: Optional[ResourceSpecification]

        addResourceSpecificationItems: ResourceSpecificationItems

    # fmt: off
    @classmethod
    def execute(cls, client: Client, input: AddResourceSpecificationItemsInput) -> addResourceSpecificationItemsData.ResourceSpecificationItems:
        variables: Dict[str, Any] = {"input": input}
        new_variables = encode_variables(variables, custom_scalars)
        response_text = client.execute(
            gql("".join(set(QUERY))), variable_values=new_variables
        )
        res = cls.addResourceSpecificationItemsData.from_dict(response_text)
        return res.addResourceSpecificationItems

    # fmt: off
    @classmethod
    async def execute_async(cls, client: Client, input: AddResourceSpecificationItemsInput) -> addResourceSpecificationItemsData.ResourceSpecificationItems:
        variables: Dict[str, Any] = {"input": input}
        new_variables = encode_variables(variables, custom_scalars)
        response_text = await client.execute_async(
            gql("".join(set(QUERY))), variable_values=new_variables
        )
        res = cls.addResourceSpecificationItemsData.from_dict(response_text)
        return res.addResourceSpecificationItems
