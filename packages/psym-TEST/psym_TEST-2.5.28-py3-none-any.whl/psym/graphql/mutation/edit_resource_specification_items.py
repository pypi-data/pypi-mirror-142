#!/usr/bin/env python3
# @generated AUTOGENERATED file. Do not Change!

from dataclasses import dataclass, field as _field
from ...config import custom_scalars, datetime
from gql_client.runtime.variables import encode_variables
from gql import gql, Client
from gql.transport.exceptions import TransportQueryError
from functools import partial
from numbers import Number
from typing import Any, AsyncGenerator, Dict, List, Generator, Optional
from time import perf_counter
from dataclasses_json import DataClassJsonMixin, config

from ..input.edit_resource_specification_items_input import EditResourceSpecificationItemsInput


# fmt: off
QUERY: List[str] = ["""
mutation editResourceSpecificationtems($input: EditResourceSpecificationItemsInput!) {
  editResourceSpecificationItems(input: $input) {
    id
    resourceSpecificationRelationship{id}
    resourceSpecification{id}
  }
}
"""
]


class editResourceSpecificationtems:
    @dataclass(frozen=True)
    class editResourceSpecificationtemsData(DataClassJsonMixin):
        @dataclass(frozen=True)
        class ResourceSpecificationItems(DataClassJsonMixin):
            @dataclass(frozen=True)
            class ResourceSpecificationRelationship(DataClassJsonMixin):
                id: str

            @dataclass(frozen=True)
            class ResourceSpecification(DataClassJsonMixin):
                id: str

            id: str
            resourceSpecificationRelationship: ResourceSpecificationRelationship
            resourceSpecification: Optional[ResourceSpecification]

        editResourceSpecificationItems: ResourceSpecificationItems

    # fmt: off
    @classmethod
    def execute(cls, client: Client, input: EditResourceSpecificationItemsInput) -> editResourceSpecificationtemsData.ResourceSpecificationItems:
        variables: Dict[str, Any] = {"input": input}
        new_variables = encode_variables(variables, custom_scalars)
        response_text = client.execute(
            gql("".join(set(QUERY))), variable_values=new_variables
        )
        res = cls.editResourceSpecificationtemsData.from_dict(response_text)
        return res.editResourceSpecificationItems

    # fmt: off
    @classmethod
    async def execute_async(cls, client: Client, input: EditResourceSpecificationItemsInput) -> editResourceSpecificationtemsData.ResourceSpecificationItems:
        variables: Dict[str, Any] = {"input": input}
        new_variables = encode_variables(variables, custom_scalars)
        response_text = await client.execute_async(
            gql("".join(set(QUERY))), variable_values=new_variables
        )
        res = cls.editResourceSpecificationtemsData.from_dict(response_text)
        return res.editResourceSpecificationItems
