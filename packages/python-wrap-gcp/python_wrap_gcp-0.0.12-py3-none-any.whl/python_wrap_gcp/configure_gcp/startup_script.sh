docker run -d -e REDIS_HOST=$HOST_IP -e APP_DB_HOST=$HOST_IP -e APP_DB_PASSWORD=$PASSWD -e REDIS_PASSWORD=$PASSWD -e MEMCACHE_HOST=$HOST_IP -e MAX_BATCH_SIZE=10 -e REDIS_QUEUE_NAME=$REDIS_QUEUE_NAME -e REDIS_TIMEOUT_SECOND=10 -e FROM_LOCAL=false --shm-size=1gb --restart=on-failure --entrypoint python3 $IMAGE $CMD